<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="priv.llf.ability.course.south.model.mybatis.UnitExt">

    <insert id="insertUnitExt" parameterType="UnitExt">
    <selectKey keyProperty="id" resultType="String" order="BEFORE">
        SELECT REPLACE(UUID(),"-","")
    </selectKey>
    INSERT INTO um_unit_ext
    VALUES (#{id},#{unitId},#{type},#{value},#{createId},#{createDate},#{available})
    </insert>

    <select id="listUnitExt" resultType="UnitExt">

        select UNE_ID id,
              UNE_UNIT_ID unitId,
              UNE_TYPE type,
              UNE_VALUE value,
              UNE_CREATOR_ID createId,
              UNE_CREATE_DATE createDate,
              UNE_AVAILABLE available
        from um_unit_ext;

    </select>

</mapper>